#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

#define MAX 100;

int main() {
    /**
     * Print the following Pattern;
     * a(1) = 1,
     * a(2) = 1,
     * a(n) = a(a(n-1)) + a(n-a(n-1))
     */

    int i, n, 
    printf("Limit Of Size N: ");
    scanf("%d", )
    for(int i=1; i<)
}